工作记录：
1.实现dataEditor应用独立化，删除应用多余的文件及配置。
2.清理jar包，删除无用的jar包引用。
3.编写系统探针，实现启动应用时探针自动检测当前数据源是否符合应用运行条件，若不符合，则启动自动修复程式，修复程式将自动创建应用需要的环境（特指所依赖的数据表及默认数据）。
4.实现用户表中对管理员账户的自动维护，若应用中的管理员账户全部被删除时，应用在重新启动的过程中会自动创建默认管理员账户！(账户名：admin，密码：admin_8888)。
5.优化应用UI界面，优化路由配置，相应地调整后端程式的结构以优化性能。
6.修复主键也能更新的BUG，已修正为：当用户修改主键时，提示用户不能修改。